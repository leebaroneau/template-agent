# This file is GENERATED by scripts/pipeline/generate-templates.mjs.
# Do not edit by hand. Edit .github/pipeline-config.yml or the skeleton at
# scripts/pipeline/templates/, then run `make pipeline-generate`.
name: Improvement
description: A hypothesis-driven change to the system
title: "[Improvement]: "
labels: ["type:experiment"]
body:
  - type: markdown
    attributes:
      value: |
        Foundation: this issue starts in `Backlog`. Add hypothesis + acceptance criteria, then run `/ready` to move it to `Selected for Development`.
  - type: dropdown
    id: domain
    attributes:
      label: Domain(s)
      description: Which business domain(s) does this affect?
      multiple: true
      options:
        - frontend
        - backend
        - infra
    validations:
      required: true
  - type: dropdown
    id: component
    attributes:
      label: Component(s)
      description: Which technical component(s) does this touch?
      multiple: true
      options:
        - auth
        - payments
        - search
    validations:
      required: true
  - type: textarea
    id: hypothesis
    attributes:
      label: Hypothesis
      description: "If we do X, then Y will improve by Z. Be specific."
      placeholder: "If we add domain-based PR labels, then PR review assignment will reduce time-to-first-review by 30%."
    validations:
      required: true
  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance Criteria
      description: "What does done look like? Checklist preferred."
      placeholder: |
        - [ ] PRs get domain labels within 30s of opening
        - [ ] No PR has more than 3 domain labels
        - [ ] Review-bot picks reviewers from the labeled domains
    validations:
      required: true
  - type: textarea
    id: test_data
    attributes:
      label: Test Data Required
      description: "What data will validate the hypothesis? (Free-form for now; will tighten when GA4/Shopify pipe lands.)"
    validations:
      required: true
  - type: textarea
    id: out_of_scope
    attributes:
      label: Out of Scope
      description: "What is explicitly NOT being addressed here?"
    validations:
      required: true
